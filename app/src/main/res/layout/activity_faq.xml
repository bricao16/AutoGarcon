<?xml version="1.0" encoding="utf-8"?>
<!--This xml file is the layout, for the FAQ of our application. This will be used as
 a template that will have a FAQ file loaded into the center of the page. This is for
 the user to understand the full FAQ of using AutoGarcon.-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:openDrawer="end"
    android:id="@+id/terms_main"
    tools:context="auto_garcon.accountstuff.Settings">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!--This displays the top tool bar. -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/xml_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorAccent" />

        <!--This text view shows the title of the page, which is FAQ. -->
        <TextView
            android:id="@+id/userTermText"
            android:layout_width="243dp"
            android:layout_height="30dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="100dp"
            android:layout_marginTop="18dp"
            android:layout_marginEnd="100dp"
            android:text="FAQ"
            android:textAlignment="center"
            android:textSize="18sp" />

        <!-- bottom nav bar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            app:itemBackground="@color/colorPrimary"
            app:itemIconTint="@android:color/white"
            app:itemTextColor="@android:color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:menu="@menu/bottom_nav_menu" />

        <!--This text view will contain the FAQ of the application and be filled in. -->
        <TextView
            android:id="@+id/textView"
            android:layout_width="318dp"
            android:layout_height="591dp"
            android:layout_above="@+id/bottom_navigation"
            android:scrollbars = "vertical"
            android:layout_below="@+id/xml_toolbar"
            android:layout_alignParentStart="true"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="50dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="43dp"
            android:layout_marginBottom="19dp"
            android:textSize="14sp"
            tools:text="hello" />
    </RelativeLayout>

    <!--This is the side navigation bar. -->
    <com.google.android.material.navigation.NavigationView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/navigationView"
        app:menu="@menu/side_nav_menu"
        app:headerLayout="@layout/side_nav_header"
        android:layout_gravity="start"
        android:background="@color/white"/>

</androidx.drawerlayout.widget.DrawerLayout>
